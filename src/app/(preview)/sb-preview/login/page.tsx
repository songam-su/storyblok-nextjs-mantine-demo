import PreviewLoginPage from '@/components/chrome/PreviewLoginPage/PreviewLoginPage';

export const dynamic = 'force-dynamic';
export const revalidate = 0;

export default function SbPreviewLoginPage() {
  return <PreviewLoginPage />;
}
