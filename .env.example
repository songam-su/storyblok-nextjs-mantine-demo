# Copy this file to `.env.local` and fill in values.
# Never commit real secrets.

# --- Storyblok tokens ---
# Required: server-side token used for preview (draft) fetches.
STORYBLOK_PREVIEW_TOKEN=

# Recommended: server-side token used for published fetches (falls back to preview token if omitted).
STORYBLOK_PUBLIC_TOKEN=

# Optional: ONLY needed if you ever do client-side Storyblok API requests.
# (Most App Router setups fetch from Storyblok server-side and do not need any NEXT_PUBLIC Storyblok tokens.)
NEXT_PUBLIC_STORYBLOK_PUBLIC_TOKEN=

# Optional: Storyblok region override (examples: eu, us).
STORYBLOK_REGION=

# Optional: used by Storyblok CLI tooling scripts.
STORYBLOK_SPACE_ID=

# Optional: only if you fetch Storyblok themes via the themes endpoint.
STORYBLOK_THEME_TOKEN=

# --- Preview route access (enterprise/QA) ---
# By default, preview routes are enabled in non-production and gated in production.
# In production, preview is allowed for allowlisted hosts (QA), Storyblok editor-originated requests,
# users with draft mode enabled, or users with a preview-auth cookie.
#
# Comma-separated list of hosts that may access preview routes in production.
# Example (local + QA): localhost:3010,127.0.0.1:3010,qa.mysite.com
PREVIEW_ALLOWED_HOSTS=

# Optional: cookie name used to grant preview access after authentication.
# Default: preview_auth
PREVIEW_AUTH_COOKIE_NAME=

# Optional: override Storyblok editor host used for request-origin checks.
# Default: app.storyblok.com
STORYBLOK_EDITOR_HOST=

# --- Site URL (metadata) ---
# Recommended: canonical base URL for metadata (OpenGraph, canonical URLs, etc.)
# Local example (matches `pnpm dev` in this repo): https://localhost:3010
# Production example: https://yourdomain.com
SITE_URL=
# Optional fallback (mainly if you need the base URL in client-side code).
# Most apps can leave this blank and just set SITE_URL.
NEXT_PUBLIC_SITE_URL=

# --- Webhooks ---
# Required only if you enable ISR revalidation webhook calls.
STORYBLOK_WEBHOOK_SECRET=

# Optional: only if you wire up the Algolia reindex webhook.
ALGOLIA_WEBHOOK_SECRET=

# --- Optional auth middleware (future / enterprise) ---
# If you later protect routes (e.g. dashboards, previews) via JWT validation.
AUTH_JWT_SECRET=
AUTH_JWT_AUDIENCE=
AUTH_JWT_ISSUER=
AUTH_REQUIRED_ROLE=
AUTH_JWKS_URL=
# Supported: HS256 | RS256 (defaults based on AUTH_JWKS_URL)
AUTH_JWT_ALG=
